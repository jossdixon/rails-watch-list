<div>
  <h1>Lists</h1>
</div>
<div>
  <%= link_to "Make A New List", new_list_path %>
</div>
<div>
  <ul>
  <% @lists.each do |list| %>
    <li> <%= link_to list.name, list_path(list) %> </li>
  <% end %>
  </ul>
</div>
